import 'package:flutter/material.dart';

class PainelBoasVindas extends StatelessWidget {
  static final String nomeRota = '/BoasVindas';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Painel Boas Vindas'),
    );
  }
}
